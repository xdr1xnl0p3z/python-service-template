{
  "license": "Apache-2.0",
  "author": "Fondeadora",
  "name": "python-service-template",
  "scripts": {
    "deploy": "sls deploy --stage=dev",
    "deploy:prod": "sls deploy --stage=production",
    "test": "python -m unittest discover",
    "venv:setup": "python3 -m venv .venv",
    "pip:dev": "pip install -r requirements-dev.txt && pip install -r requirements.txt",
    "pip": "pip install -r requeriments.txt"
  },
  "devDependencies": {
    "serverless-offline": "^4.1.4",
    "serverless-offline-sqs": "^1.6.1",
    "serverless-plugin-git-variables": "^3.3.2",
    "serverless-plugin-warmup": "^4.7.0-rc.1",
    "serverless-prune-plugin": "^1.4.0",
    "serverless-python-requirements": "^4.2.5"
  }
}
